# vault/

Punto de montaje local del vault de Obsidian usado en desarrollo. Las notas reales del usuario están en `.gitignore` — nunca se commitean. La única excepción es `notas-de-prueba.md`, que sí se trackea a propósito como fixture mínimo para validar el pipeline de RAG (Fase 2) sin depender de un vault real.
