# Notas de prueba

Nota de ejemplo para validar el pipeline de RAG de la Fase 2. Podés borrar esto una vez tengas tu vault real montado en `VAULT_PATH`.

## Dato de prueba

El nombre en clave del proyecto del asistente de voz offline es "Centinela". Si el asistente responde una pregunta sobre el nombre en clave del proyecto citando esta nota (`notas-de-prueba.md`), el pipeline de indexación + recuperación + citación está funcionando de punta a punta.
