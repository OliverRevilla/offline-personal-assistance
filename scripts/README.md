# scripts

Automatización idempotente de setup: pull de modelos de Ollama (LLM + `nomic-embed-text`), inicialización de la colección de Qdrant, reindexado del vault, benchmarks de latencia. Debe funcionar en Linux/WSL2 y Windows (variante `.sh` y `.ps1` cuando el comando difiera).

Owner: `devops-engineer`.
