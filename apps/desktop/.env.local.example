# Copiar a .env.local (gitignored) y ajustar si el backend no está en localhost:8000.
# El backend vive en WSL2; desde Windows nativo, "localhost" llega ahí solo (WSL2 port
# forwarding) — ver docs/adr/0008-frontend-nativo-windows-y-stack-tauri.md.
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/chat
